记录所有的相机轨迹，用State保存

每个State包含对应镜头的信息

让模型生成相机的树形依赖

每个相机选择第一个state生成场景，找到依赖

# 个人笔记 (2024-01-15)
# 疑问：树形依赖的深度是否需要限制？防止循环依赖
# 考虑用 max_depth=5 作为默认上限

# 更新 (2024-01-22)
# 测试了 max_depth=5，对于复杂场景感觉不够用
# 改为 max_depth=8，同时需要加循环检测（visited set）
# 注意：深度过大会导致生成时间显著增加，8 应该是比较合理的上限
